         // if (nbx == nrx && nby == nry)
            // {
            //     if (abs(nrx - cur.rx) + abs(nry - cur.ry) > abs(nbx - cur.bx) + abs(nby - cur.by))
            //     {
            //         nrx -= dx[i];
            //         nry -= dy[i];
            //     }
            //     else
            //     {
            //         nbx -= dx[i];
            //         nby -= dy[i];
            //     }
            // }